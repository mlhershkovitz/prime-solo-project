SELECT "dog"."name" FROM "attributes" 
JOIN "dog" ON "dog"."id" = "attributes"."dog_id"
WHERE "female" = TRUE || 
WHERE "male" = TRUE || 
WHERE "xsmall" = TRUE || 
WHERE "small" = TRUE || 
WHERE "medium" = TRUE || 
WHERE "large" = TRUE || 
WHERE "xlarge" = TRUE || 
WHERE "shedding" = TRUE || 
WHERE "not shedding" = TRUE || 
WHERE "activity-xlow" = TRUE || 
WHERE "activity-low" = TRUE || 
WHERE "activity-medium" = TRUE || 
WHERE "activity-high" = TRUE || 
WHERE "activity-xhigh" = TRUE || 
WHERE "train-basics" = TRUE || 
WHERE "train-easy" = TRUE || 
WHERE "train-hard" = TRUE || 
WHERE "dogs" = TRUE || 
WHERE "cats" = TRUE || 
WHERE "kids" = TRUE || 
WHERE "other-animals" = TRUE || 
WHERE "single-animal" = TRUE || 
WHERE "dog-park" = TRUE || 
WHERE "crowds" = TRUE || 
WHERE "apartment" = TRUE || 
WHERE "physical-fence" = TRUE || 
WHERE "leash-walking" = TRUE || 
WHERE "dog_id" = TRUE

SELECT "dog"."name" FROM "attributes" 
                            JOIN "dog" ON "dog"."id" = "attributes"."dog_id"

                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE || 
                            WHERE  = TRUE `;


male
        xsmall
        small
        medium
        large
        xlarge
        shedding
        not shedding
        activity-xlow
        activity-low
        activity-medium
        activity-high
        activity-xhigh
        train-basics
        train-easy
        train-hard
        dogs
        cats
        kids
        other-animals
        single-animal
        dog-park
        crowds
        apartment
        physical-fence
        leash-walking];